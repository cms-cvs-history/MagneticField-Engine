process MAGNETICFIELDTEST = {
    source = EmptySource {}

    untracked PSet maxEvents = {untracked int32 input = 1}

    // Keep this to check we are able to override it. 
    // Must specify to override the EmptyESSource defined in uniformMagneticField.cfi
    include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"


    include "MagneticField/Engine/data/uniformMagneticField.cfi"

    es_prefer = UniformMagneticFieldESProducer{}

    module testMagneticField = testMagneticField {}
    
    path p = {testMagneticField}

}
